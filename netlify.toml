[build]
  # Install dependencies ignoring peer dependency conflicts, then build
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

# SPA routing: serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
